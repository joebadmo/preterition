(ns repo-store.config)

(def path-prefix "./repos/")
(def username "joebadmo")
(def repository "joe.xoxomoon.com-content")
(def repo (str username "/" repository))
(def path (str path-prefix repo))
(def branch "repo-store")
